import ProfilePage from "@/components/profile/account";

const Profile = () => {
  return (
    <div className="bg-sky-blue h-screen w-screen flex justify-center">
      <ProfilePage />
    </div>
  );
};

export default Profile;
